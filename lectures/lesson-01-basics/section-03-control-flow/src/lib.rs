//! Section: Control Flow
//!
//! Complete the functions below using `if`, `match`, and loops.

/// Return true if `n` is even.
pub fn is_even(n: i32) -> bool {
    // Hint: use the remainder operator `%`
    unimplemented!("return true when n % 2 == 0")
}

/// Classic fizz_buzz rules:
/// - multiples of 3 => "Fizz"
/// - multiples of 5 => "Buzz"
/// - multiples of 15 => "FizzBuzz"
/// - otherwise => the number itself
pub fn fizz_buzz(n: i32) -> String {
    // Hint: check 15 first, then 3/5
    unimplemented!("implement fizz_buzz using branching")
}

/// Sum from 1 to `n` inclusive.
pub fn sum_up_to(n: u32) -> u32 {
    // Hint: a `for` loop over 1..=n works well
    unimplemented!("accumulate a sum using a loop")
}

